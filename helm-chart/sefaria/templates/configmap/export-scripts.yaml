{{- if .Values.export.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: export-scripts-{{ .Values.deployEnv }}
  labels:
    deployEnv: "{{ .Values.deployEnv }}"
    {{- include "sefaria.labels" . | nindent 4 }}
data:
  upload-to-gcs.sh: |-
    #!/usr/bin/env bash
    set -euo pipefail
    set -x

    echo "Syncing export data to gs://${BUCKET}/current/"
    gcloud storage rsync -r --delete-unmatched-destination-objects \
      /export/ "gs://${BUCKET}/current/"
    echo "Done. $(date -u +%Y-%m-%dT%H:%M:%SZ)" > /tmp/last_export.txt
    gcloud storage cp /tmp/last_export.txt "gs://${BUCKET}/current/last_export.txt"
    echo "Upload complete."
{{- end }}
